import { useEffect, useMemo, useState } from 'react';
import { useAuth0 } from '@auth0/auth0-react';
import { Lock, MessageCircle, Plus, Shield, UserRound, Paperclip, Send, LogOut } from 'lucide-react';
import { makeSupabase } from './lib/supabase';
import { createChatKey, decryptText, encryptText, exportKey, importKey } from './lib/crypto';
import { getOrCreateIdentity, unwrapChatKey, wrapChatKey } from './lib/crypto-keys';

const ALPHABET = '0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ';
const ID_LENGTH = 12;
type Chat = { id:string; other_user_id:string; other_name:string; hidden:boolean; password_protected:boolean; save_history:boolean; key?:CryptoKey };
type Message = { id:string; sender_id:string; ciphertext:string; created_at:string };
const randomUserId = () => { const b=crypto.getRandomValues(new Uint8Array(ID_LENGTH)); return Array.from(b,x=>ALPHABET[x%ALPHABET.length]).join(''); };

export default function App(){
 const {isLoading,isAuthenticated,loginWithRedirect,logout,user,getIdTokenClaims}=useAuth0();
 const [tab,setTab]=useState<'chat'|'profile'>('chat'),[chats,setChats]=useState<Chat[]>([]),[selected,setSelected]=useState<Chat|null>(null),[messages,setMessages]=useState<Message[]>([]),[draft,setDraft]=useState(''),[showAdd,setShowAdd]=useState(false),[targetId,setTargetId]=useState(''),[displayName,setDisplayName]=useState(user?.name??''),[userCode,setUserCode]=useState(''),[hiddenMode,setHiddenMode]=useState(false),[appLocked,setAppLocked]=useState(false),[notice,setNotice]=useState('');
 const supabase=useMemo(()=>isAuthenticated?makeSupabase(async()=> (await getIdTokenClaims())?.__raw):null,[getIdTokenClaims,isAuthenticated]);
 useEffect(()=>{if(supabase&&user?.sub){void loadProfile();void loadChats();}},[supabase,user?.sub]);
 async function loadProfile(){if(!supabase||!user?.sub)return;const {data}=await supabase.from('profiles').select('display_name,user_code').eq('auth0_sub',user.sub).maybeSingle();if(!data){const identity=await getOrCreateIdentity();const {data:created}=await supabase.from('profiles').insert({auth0_sub:user.sub,display_name:user.name??'User',user_code:randomUserId(),public_key:identity.publicJwk}).select('display_name,user_code').single();if(created){setDisplayName(created.display_name);setUserCode(created.user_code);}}else{setDisplayName(data.display_name);setUserCode(data.user_code);await getOrCreateIdentity();}}
 async function loadChats(){if(!supabase||!user?.sub)return;const {data}=await supabase.from('chat_members').select('chat_id,hidden,chats(id,password_protected,save_history,chat_members(auth0_sub,display_name,user_code))').eq('auth0_sub',user.sub);setChats((data??[]).flatMap((row:any)=>{const other=(row.chats?.chat_members??[]).find((m:any)=>m.auth0_sub!==user.sub);return other?[{id:row.chat_id,other_user_id:other.user_code,other_name:other.display_name,hidden:row.hidden,password_protected:row.chats.password_protected,save_history:row.chats.save_history}]:[]}));}
 async function addPerson(){const code=targetId.trim();if(code.length!==ID_LENGTH)return setNotice('User ID must be exactly 12 characters.');if(!supabase||!user?.sub)return;const {data:target}=await supabase.from('profiles').select('auth0_sub,display_name,user_code,public_key').eq('user_code',code).maybeSingle();if(!target)return setNotice('No user found with that ID.');if(target.auth0_sub===user.sub)return setNotice('You cannot add yourself.');const identity=await getOrCreateIdentity(),chatKey=await createChatKey(),raw=await exportKey(chatKey);const ownWrapped=await wrapChatKey(raw,identity.privateKey,identity.publicJwk),targetWrapped=await wrapChatKey(raw,identity.privateKey,target.public_key);const {data:chat,error}=await supabase.from('chats').insert({created_by:user.sub,password_protected:false,save_history:true}).select('id').single();if(error||!chat)return setNotice(error?.message??'Could not create chat.');const {error:memberError}=await supabase.from('chat_members').insert([{chat_id:chat.id,auth0_sub:user.sub,display_name:displayName,user_code:userCode,hidden:false,encrypted_key:ownWrapped,key_sender_public:identity.publicJwk},{chat_id:chat.id,auth0_sub:target.auth0_sub,display_name:target.display_name,user_code:code,hidden:false,encrypted_key:targetWrapped,key_sender_public:identity.publicJwk}]);if(memberError)return setNotice(memberError.message);setSelected({id:chat.id,other_user_id:code,other_name:target.display_name,hidden:false,password_protected:false,save_history:true,key:chatKey});setShowAdd(false);setTargetId('');setNotice('');await loadChats();}
 async function openChat(chat:Chat){if(!supabase||!user?.sub)return;const identity=await getOrCreateIdentity();const {data}=await supabase.from('chat_members').select('encrypted_key,key_sender_public').eq('chat_id',chat.id).eq('auth0_sub',user.sub).single();if(!data)return;try{const raw=await unwrapChatKey(data.encrypted_key,identity.privateKey,data.key_sender_public),key=await importKey(raw);setSelected({...chat,key});if(chat.save_history){const {data:rows}=await supabase.from('messages').select('id,sender_id,ciphertext,created_at').eq('chat_id',chat.id).order('created_at',{ascending:true});setMessages(rows??[]);}else setMessages([]);}catch{setNotice('This chat cannot be unlocked on this device.');}}
 async function sendMessage(){if(!draft.trim()||!selected?.key||!supabase||!user?.sub)return;const ciphertext=await encryptText(draft.trim(),selected.key);await supabase.from('messages').insert({chat_id:selected.id,sender_id:user.sub,ciphertext});setDraft('');if(selected.save_history){const {data}=await supabase.from('messages').select('id,sender_id,ciphertext,created_at').eq('chat_id',selected.id).order('created_at',{ascending:true});setMessages(data??[]);}}
 async function saveProfile(){if(supabase&&user?.sub){await supabase.from('profiles').update({display_name:displayName}).eq('auth0_sub',user.sub);setNotice('Profile saved.');}}
 if(isLoading)return <div className="center">Loading secure session…</div>;
 if(!isAuthenticated)return <div className="landing"><div className="brand"><Shield size={28}/> Encrypted Chat</div><h1>Private by design.</h1><p>Messages are encrypted in your browser before they reach Supabase.</p><button className="primary" onClick={()=>loginWithRedirect()}>Create account / Log in</button></div>;
 if(appLocked)return <div className="center"><div className="lock-card"><Lock size={30}/><h2>App locked</h2><button className="primary" onClick={()=>setAppLocked(false)}>Unlock</button></div></div>;
 const visible=chats.filter(c=>hiddenMode?c.hidden:!c.hidden);
 return <div className="app-shell"><aside className="sidebar"><div className="brand"><Shield size={22}/> Encrypted Chat</div><div className="privacy-badge"><Lock size={15}/> End-to-end encrypted</div><nav><button className={tab==='chat'?'nav active':'nav'} onClick={()=>setTab('chat')}><MessageCircle size={18}/> Chat</button><button className={tab==='profile'?'nav active':'nav'} onClick={()=>setTab('profile')}><UserRound size={18}/> Profile</button></nav><div className="sidebar-bottom"><button className="nav" onClick={()=>setAppLocked(true)}><Lock size={18}/> Lock app</button><button className="nav danger" onClick={()=>logout({logoutParams:{returnTo:window.location.origin}})}><LogOut size={18}/> Log out</button></div></aside><main className="main">{tab==='chat'?<><header className="topbar"><div><span className="eyebrow">PRIVATE MESSAGING</span><h1>{hiddenMode?'Hidden chats':'Chats'}</h1></div><button className="icon-button" onClick={()=>setShowAdd(true)}><Plus/></button></header><section className="chat-layout"><div className="chat-list">{visible.length===0?<div className="empty"><MessageCircle size={32}/><p>No chats yet.</p><button className="secondary" onClick={()=>setShowAdd(true)}>Add someone</button></div>:visible.map(c=><button className={selected?.id===c.id?'chat-row selected':'chat-row'} key={c.id} onClick={()=>void openChat(c)}><div className="avatar">{c.other_name.slice(0,1).toUpperCase()}</div><div><strong>{c.other_name}</strong><small>{c.other_user_id}</small></div>{c.password_protected&&<Lock size={14}/>}</button>)}</div><div className="conversation">{!selected?<div className="empty large"><Shield size={42}/><h2>Your messages stay encrypted</h2><p>The server stores ciphertext for message bodies.</p></div>:<><div className="conversation-head"><div><strong>{selected.other_name}</strong><small>{selected.other_user_id}</small></div><div className="security-label"><Lock size={14}/> encrypted</div></div><div className="messages">{messages.map(m=><div className={m.sender_id===user.sub?'bubble mine':'bubble'} key={m.id}><AsyncMessage ciphertext={m.ciphertext} keyRef={selected.key!}/></div>)}</div><div className="composer"><button className="icon-button"><Paperclip size={18}/></button><input value={draft} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>e.key==='Enter'&&void sendMessage()} placeholder="Write an encrypted message…"/><button className="send" onClick={()=>void sendMessage()}><Send size={18}/></button></div></>}</div></section></>:<Profile displayName={displayName} setDisplayName={setDisplayName} userCode={userCode} save={saveProfile} hiddenMode={hiddenMode} setHiddenMode={setHiddenMode} setAppLocked={setAppLocked}/>}</main>{showAdd&&<div className="modal-backdrop"><div className="modal"><button className="modal-close" onClick={()=>setShowAdd(false)}>×</button><h2>Add someone</h2><p>Enter their 12-character User ID.</p><input autoFocus value={targetId} maxLength={12} onChange={e=>setTargetId(e.target.value)} placeholder="aB7x…"/><button className="primary" onClick={()=>void addPerson()}>Create encrypted chat</button>{notice&&<div className="notice">{notice}</div>}</div></div>}</div>;
}
function AsyncMessage({ciphertext,keyRef}:{ciphertext:string;keyRef:CryptoKey}){const [text,setText]=useState('Decrypting…');useEffect(()=>{void decryptText(ciphertext,keyRef).then(setText).catch(()=>setText('Unable to decrypt'));},[ciphertext,keyRef]);return <>{text}</>;}
function Profile({displayName,setDisplayName,userCode,save,hiddenMode,setHiddenMode,setAppLocked}:any){return <div className="profile"><header className="topbar"><div><span className="eyebrow">ACCOUNT</span><h1>Profile & privacy</h1></div></header><div className="profile-card"><div className="avatar big">{displayName.slice(0,1).toUpperCase()||'U'}</div><div><h2>{displayName||'User'}</h2><small>User ID: <code>{userCode}</code></small></div></div><section className="settings"><label>Display name<input value={displayName} onChange={e=>setDisplayName(e.target.value)}/></label><button className="primary compact" onClick={save}>Save profile</button><div className="setting"><div><strong>Hidden chats</strong><small>Keep hidden chats out of the normal chat list.</small></div><button className={hiddenMode?'toggle on':'toggle'} onClick={()=>setHiddenMode(!hiddenMode)}><span/></button></div><div className="setting"><div><strong>App lock</strong><small>Lock the current app session.</small></div><button className="secondary compact" onClick={()=>setAppLocked(true)}>Lock now</button></div><div className="privacy-box"><Shield size={20}/><div><strong>Privacy model</strong><p>Message bodies are encrypted in the browser with AES-GCM. Chat keys are wrapped with ECDH public keys. Supabase stores ciphertext and routing metadata; no analytics SDK is included.</p></div></div><div className="setting disabled"><div><strong>Attachments</strong><small>Planned: encrypted attachments with automatic deletion after 5 minutes.</small></div></div></section></div>}
